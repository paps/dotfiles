if [ -f ~/.paps/x/do_not_lock ] ; then
    xrdb -load -DLOCK=false ~/.Xresources
else
    xrdb -load -DLOCK=true ~/.Xresources
fi

~/.paps/fonts/update.sh

export MOZ_USE_XINPUT2=1

exec dbus-launch --exit-with-session openbox-session
